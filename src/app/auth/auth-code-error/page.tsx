import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Auth Code Error",
    description: "Auth Code Error",
};

const AuthCodeError = () => {
    return <div>AuthCodeError</div>;
};

export default AuthCodeError;